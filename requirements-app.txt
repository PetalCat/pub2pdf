# Desktop app (pub2pdf_gui.py) dependencies. The CLI/engine (pub2pdf.py) needs
# only pywin32 for the Publisher COM path; these are extra, for the GUI shell.
customtkinter>=5.2
tkinterdnd2>=0.4
pywin32>=306
pillow>=10.0        # monoline UI icons are drawn with PIL at runtime
